# ai0-video-creator (智能视频创作工作站) 用户使用手册

欢迎使用 `ai0-video-creator`（智能视频创作工作站）！本手册旨在协助您全面、深入地熟悉系统各项高级功能，掌握从**“AI 原生文本创意 ─ 多模态素材映射 ─ 高精多轨时间轴 ─ 声音克隆与口型同步”**的全链路专业级视听创作管线。

---

## 一、 🚀 快速冷启动与架构认知

`ai0-video-creator` 是一款专门为独立创作者、自媒体极客、外语教育机构及多语种音视频编译团队打造的**全链路多模态视频创作桌面套件**。

### 1. 双运行态环境感知底座
系统具备极强的**环境感知架构**，能根据当前宿主环境智能切换运行模式：
* **桌面端模式 (Tauri v2)**：推荐专业创作者使用。通过 C++ WebView 容器直接唤醒您本地的硬件加速推理机、SQLite 数据库以及本地磁盘文件系统，支持海量项目本地资产的无损保存，并完美联动本地 Python 虚拟环境与 CUDA 显卡算力。
* **Web 浏览器模式 (Web Preview)**：轻量化预览及云端直连模式。运行于沙箱隔离环境中，本地数据库将自动安全降级至 LocalStorage 及 IndexedDB 进行卡片字段与全局状态的暂存，调用远程 Gemini/Qwen 接口实现无配置即开即用。

### 2. 主仪表盘与封面工坊
* **项目生命周期管理**：在项目主页（Dashboard）可直观分类与搜索当前持有的所有视频项目、生词卡、横/竖屏故事项目，一键进行新建或删除。
* **双模封面渲染工坊 (Cover Engine)**：
  * **`z-image-turbo` 闪电草稿核**：2.5秒极速响应出图，专为高频创意调整、快速生成草稿设计。
  * **`qwen-image-2512` 电影排版核**：生成时间 8-15 秒，提供超写实光影材质、电影级排版与双语文字融合。
  * **自动持久化机制**：生成的封面素材会物理转写为本地 PNG 图片资产存入项目 `/cover/` 物理目录并持久化存储，无缝规避前端大内存溢出（Out of Memory）风险。

---

## 二、 🎬 八大核心功能版块全景实操

当您进入任何一个项目后，可以通过顶部的导航切分八大工坊面板：

### 1. 项目详情总览 (`details` - Project Details)
* **功能定位**：项目概览与视频输出预览。
* **视觉核心预览 (Visual Core Preview)**：实时监测项目当前的视频编译进度，集成自适应 base64 解析与 fallback 资产路径转换。在未装载或加载中状态下自动提供优雅占位，规避传统播放器黑屏与报错问题。

### 2. 单词背诵与卡片工坊 (`words` - Word Management)
针对外语点读或背词视频细分方向设立的多媒体矩阵卡片工坊：
* **A-Z 字母滑动检索面板**：左侧设计高灵敏度的 A-Z 悬停滑动边栏，支持海量单词毫秒级定位。
* **例句音标细粒度编辑**：完美记录单词、国际音标（IPA）、中英互译、双语造句及真实语境。
* **AI 提示词一键爆发**：内置 Qwen 提示词生产流，为抽象词意（例如 `Serendipity` 缘分分合）一键生成契合 `LTX-2.3` 或是 `CogVideo` 运动规律的写实提示词，并一键调用 4 秒小视频模型渲染动态背景视频。

### 3. 脚本合成工坊 (`script` - Script Editor)
* **场景分割与 AI 智能断句**：输入长文本后，可一键调用高级大语言模型进行“AI 智能断句”，将文本拆分为带有精准旁白/对白、时间估计的分镜卡片。
* **场景名称个性化定制 (Scene Name Editing)**：新增场景名称（Scene Title）实时修改对话框。点击场景卡片标题旁的编辑图标，可自由定制每个镜头的主题（例如：`主角登场`、`远景拉伸`），点击保存即可立即同步至本地数据库。
* **声效与多引擎 TTS**：集成火山字节声学、Qwen3-TTS，为每句旁白或角色台词合成高保真、带情感表达的配音，并计算发音波形长度。

### 4. 视觉资产仓库 (`visuals` - Visuals Library)
* **提示词一致性规则 (Prompt Consistency)**：通过定义 **“触发关键字” (Trigger Keywords, 例如 `@Hero`)**，系统在 AI 渲染时会自动匹配合并视觉库中对应角色的精细描述，确保多镜头转换下人物长相、衣服、环境的一致性，拒绝频繁“变脸”。
* **动态背景自适应**：增强对未加载或本地缺失图片的 fallback 图像自愈加载，提供电影海报级的平滑占位体验。

### 5. 声音克隆引擎 (`audio` - Audio Engine)
* **自定义音色克隆 (Voice Clone)**：支持上传 5-10 秒的参考干声音频，一键生成物理声学特征模型，并在多角色台词中直接调用。
* **环境环境声与 BGM 叠轨**：轻松上传背景音乐与动态音效，控制多声轨音量淡入淡出。

### 6. 高精多轨时间轴编辑器 (`timeline` - Timeline Editor)
提供业内领先的音视频多轨协同编辑画布，包含“画面轨”、“音频轨”和“字幕轨”：
* **实时播放器渲染画布 (Preview Canvas)**：直接预览多轨混合合成效果，支持 0.01 秒精确拖拽、字幕段拆分、追加与对齐。
* **字幕外观及位置超极限定制 (Subtitle Styling)**：
  * **多轴位置选择 (Position)**：支持快速设定为“底部 (Bottom)”、“顶部 (Top)”、“居中 (Middle)”，并完美支持“自定义坐标 (Custom)”。开启自定义坐标后，可自由输入精确高度（例如 `85%` 或 `420px`）。
  * **自适应宽度 (Width)**：宽度输入框支持输入百分比（如 `80%`）或 `auto`。当设为 `auto` 时，字幕容器将以像素级精度**自动适应并紧密包裹文字大小**。
  * **高保真外观控制**：内置精确到像素的字体大小 (FontSize) 调节、十六进制/调色板文本颜色 (TextColor) 选择。
  * **多重背景样式 (Background Style)**：
    1. *默认半透明磨砂*：高对比度、带毛玻璃背景的经典圆角半透明面板。
    2. *无背景 (None)*：去除多余底色，文本底层自动注入强对比黑色外边框阴影（Text Shadow Outline），确保在任何复杂视频画面下依然字字清晰。
    3. *上传自定义 PNG 背景*：**支持上传自定义透明 PNG 贴图**！上传后，系统会自动拉伸并让 PNG 贴图作为背景框，**完美自适应包裹文字大小**。

### 7. 视频翻译与声唇对齐中枢 (`translation` - Video Translation)
* **SRT 智能时间对齐**：视频一键导入，毫秒级断句与 SRT 翻译轴提取。
* **Colloquial (口语化) 译配翻译**：调用大语言大模型结合情景进行俗语化口语代换，消除死板僵硬的书面语感。
* **音唇同步 (LipSync)**：集成 Wav2Lip 模型，读取翻译配音后的音频波形，逐帧重对面部特定多边形实施神经形态贴合，实现人物嘴型 100% 对齐。

### 8. 成果输出大师 (`export` - Export Center)
* **一键渲染合成**：调用 Web Simulation 极速仿真逻辑，或通过 Tauri 调起物理 FFmpeg 引擎。
* **硬/软字幕多格式烧录**：支持将高定字幕以内嵌硬字幕（`burnt` via libass，推荐，绝不丢失样式）或内封装软字幕（`soft` container，播放器自配）形式随片导出，搭配 `ultrafast` 到 `slow` 多档压缩 Preset。

---

## 三、 ⚙️ 系统高阶设置与性能优化指南

点击主界面右上角的 **⚙️ 系统设置** 即可对本地硬件、路径进行调优：

1. **Python 原生执行环境绑定**：
   * 避免由于拼写或转义造成的路径报错。点击 **“Browse”** 将调起原生系统文件管理器，精确定位到 Anaconda 或 Python 虚拟环境执行文件（如 `venv/Scripts/python.exe`）。
2. **CUDA 推理卡槽隔离 Assignment**：
   * 针对多卡配置的主机，可在 “CUDA DEVICE ID” 中指定参与 AI 推理与重绘的物理 GPU 卡槽（如 GPU `1`）。系统在拉起 Wave2Lip 或 ComfyUI 推理时会自动注入 `CUDA_VISIBLE_DEVICES=1` 变量，保障前台流畅操作。
3. **运算多线程保护阀门**：
   * 滑动设置“并发执行最大核心数”（推荐设定在 `4 - 6`），在进行批量生词卡片渲染、长视频高密集切片翻译时，有效压制显卡温升，规避系统崩溃或内存溢出。

---

## 四、 🛠️ 常见故障排除与数据自愈恢复

### 1. 数据库被锁/迁移报错
在使用 Tauri 本地 SQLite 时，若遭遇强退、多版本热更新，可能在日志或终端看到 `(code: 5) database is locked` 或 `migration hash mismatch` 错误。
* **两阶段物理级数据自愈模块 (Auto-Healing)**：系统在初始化阶段若捕获到此类崩溃，前端将下发物理删除指令，自动清空位于电脑特定 AppLocalData / AppData 路径下的 `main.db` 冲突旧表，并由 Rust 进程重新安全载入一干二净的最新数据表，杜绝系统白屏。

### 2. 双轨自动热备份机制 (Hybrid Backups)
* **项目资产永不丢失**：当发生数据库重置时，您辛苦创建的项目、生词和剧本并不会丢失。
* 系统内置双轨热同步：您在工作区中的每次修改，前台均会打包一份加密 JSON 整库树热备份持久化在 SQLite 全局 `app_settings` 表（或浏览器的 LocalStorage 中）。当 main.db 重构复原后，系统一秒将其解析并**全量还原**回您的当前列表，让创作者无后顾之忧。

### 3. 日志诊断
如果遇到生图、合成中断，可查看工作区根目录下的 `./logs/` 日志文件夹。日志中记录了包括 Unified DB Path、Ollama Gateway、ComfyUI WebSocket 连接帧及系统写入权限拦截在内的全套详细运行日志，可助您快速定位故障原因。
